sqlite> SELECT COUNT(DISTINCT Gender)
   ...> FROM persons;
+------------------------+
| COUNT(DISTINCT Gender) |
+------------------------+
| 8                      |
+------------------------+
sqlite> 
